{
  "name": "vite-electron-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:electron-main": "vite build --mode production --target electron-main",
    "electron": "electron ."
  },
  "devDependencies": {
    "electron": "^18.0.0",
    "vite": "^3.0.0"
  }
}
